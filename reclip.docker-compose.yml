services:
  reclip:
    image: ghcr.io/as4ajaysoni/reclip:latest
    container_name: reclip
    pull_policy: always
    ports:
      - "8899:8899"
    environment:
      - HOST=0.0.0.0
    volumes:
      - /mnt/DRIVE/downloads:/app/downloads   # Replace required path
    restart: unless-stopped
